# Waveter is a static site: the station catalogue (Radio Browser) is queried
# directly from the browser, so there are no functions or API redirects.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
